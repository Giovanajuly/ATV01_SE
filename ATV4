void setup() {
  Serial.begin(9600);
}
 
void loop() {
  int leitura = analogRead(A0);                 
  float a = leitura * (5.0 / 1023.0);     
  float temperatura = (a - 0.5) * 100.0;  
  Serial.println(temperatura);                 
  delay(1000);                                
}
